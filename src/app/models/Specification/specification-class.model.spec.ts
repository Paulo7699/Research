import { SpecificationClass } from './specification-class.model';

describe('SpecificationClass', () => {
  it('should create an instance', () => {
    expect(new SpecificationClass()).toBeTruthy();
  });
});
